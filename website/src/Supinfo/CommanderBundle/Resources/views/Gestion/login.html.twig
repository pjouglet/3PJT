<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Panneau d'administration</title>
    <!-- jQuery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>
    <!-- Bootstrap -->
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="{{ asset('bundles/supinfocommander/css/admin/style.css') }}"/>
    <script src="{{ asset('bundles/supinfocommander/js/admin/login.js') }}"></script>
</head>
<body>
<div class="container login-container">
    <div class="row">
        <div class="col-sm-3"></div>
        <div class="col-sm-6">
            <div class="panel panel-default">
                <div class="panel-body">
                    {% if login_error is defined %}
                        <div class="col-sm-12">
                            <div class="alert alert-danger">
                                <p>
                                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                                    Erreur lors de la connection. Vérifiez vos identifiants.
                                </p>
                            </div>
                        </div>
                    {% endif %}
                    <h4 id="shop-name">Train commander</h4>
                    <form role="form" method="post">
                        <div class="form-group">
                            <label for="email">Adresse e-mail</label>
                            {{ form_widget(login_form.email, { 'id': 'email' }) }}
                        </div>
                        <div class="form-group">
                            <label for="password">Mot de passe</label>
                            {{ form_widget(login_form.password, { 'id': 'password' }) }}
                        </div>
                        <div class="form-group">
                            <button id="login_button" value="true" class="btn btn-primary" name="login_button" type="submit">Se connecter</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>